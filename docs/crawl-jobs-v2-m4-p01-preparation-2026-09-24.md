# P01 preparation — 2026-09-24

**Result: PASS for P01-specific artifact preparation.** Fresh exact-artifact
owner approval and a separate execution decision were the handoff gates.
The subsequent separately authorized [P01 attempt passed](crawl-jobs-v2-m4-p01-run-2026-09-24.md);
its approval is consumed and scoped evidence review is next.

## Revision, control and CI

Selected execution revision: `963b67b73e2cd67ff73f559fa9984813b9d5946a`, the
published head of [PR #13](https://github.com/fullerkris/mifolyo/pull/13).
PR #13 merged at **2026-09-24 16:57:46 UTC** as
`2b2dd0b240984e379f5464ce7066fb5a31c904f9`. The published head, tested PR merge
`70321f77e64ba3fbcd995bcb14668748439a24ca` and final merge all have tree
`36a750d55f51003e6f67ef446a66335fdb812a61`. The checkout remains on the selected
published head, with clean tracked execution inputs.

All **81 reviewed source hashes** and **15 recipe hashes** match inventory
`dbe881b2c269535b633188986c6f3adbdac0226df23528290bc4554c93277113`.
The accepted [PC01 control review](crawl-jobs-v2-m4-pc01-evidence-review-2026-09-24.md),
`b416011bf828111cecf2311a3adf1243e8e6b2ffe1c3ace9bce8c47ae8e461bb`, and all its
run exports were rechecked. Execution source, images and established authority
policies are unchanged; P01 applies the separately specified presence delta.

All **14 required checks** pass under unchanged strict branch protection:

- [Required Checks, run 36022916908](https://github.com/fullerkris/mifolyo/actions/runs/36022916908).
- [Unit Tests, run 36022916893](https://github.com/fullerkris/mifolyo/actions/runs/36022916893).

Both runs bind the selected head. Downloaded eight-shard reports match the local
compiled inventory: **475 roots**, 474 passed and only the permitted optional
`TestJobLuaNativeFactoryParity` skipped. Their tested merge tree matches above.
Downloaded amd64 image evidence also matches the 62 execution files, 15 recipes,
source identities and memory/isolation predicates. That CI image report selects
PC01; the local artifacts below explicitly select P01.

Private CI gate: `ci-gate.json`, SHA-256
`223a6ae284ec1cdaf99265daf24c70f9d81d081300a32cf540ff8303c8b456a2`, under the
private workspace named below, alongside original downloaded shard/image reports.

## Exact selected artifacts

| Item | Identity |
|---|---|
| Case | `ledger-candidate-compat-present-v1` |
| Scenario | `ledger-candidate-compat-present` |
| Platform / intended operator | Linux/arm64 / `fullerkris` |
| Harness and stand-in image | `sha256:51bc4896057b8015e1bb67ff7e57448ed6356449c98a6df9100c06693de8e265` |
| Redis 7.4.11 image | `sha256:24e81cffaba832bcd71068a6ff772a531076bafdbb1d684195766ae9b6511f5c` |
| P01 plan | `503f6ae40e0bd2ce7e833b90eb8c9d62b48d391972b508fb06b243ae0b33d79c` |
| P01 recipe | `7123ed770cb9c900319f412356652e7cb27eef6c3f2704818d3dee402fa4ee74` |
| Image validation | `b66b1d27ef92935dc12b22a91a57b109297b734cf64c167ee98e362bad7affd1` |
| Independent postcheck | `9c110f02336b56db345dadbbde4b947859fe1394cb3a8c666c2edeba0dd99e4b` |

The [exact preparation exports](evidence/m4-p01-image-prep-2026-09-24/README.md)
were generated by `scripts/prepare-crawl-jobs-v2-images.py` with
`--case ledger-candidate-compat-present-v1`. Metadata fixture
`e1b6c7ed5b04d3f8ce02e16bba94587f` passed all four stopped-role admissions,
with **zero metadata-container starts**. Separate networkless Redis-version and
init/executor Python checks passed. Their memory peaks were **48,861,184** and
**48,943,104** bytes, below 128/256 MiB limits; all 62 files and 15 recipes match.
These image-check peaks are not maximum-shape runtime acceptance measurements.

The independent postcheck at **17:46:00.102698 UTC** found all four exact metadata
containers and both volumes absent, with empty fixture/image-check listings.
No P01 acceptance Redis server was started during preparation.

## Bounded intended execution

P01 uses one ready job and adds only a correctly typed compatibility-marker hash
at `mifolyo:contracts:candidate`: 27 direct setup keys, 58 possible protocol
positions, durability BOOT-owned. The negative-stored-state ledger profile loads
only canonical `CJ2_APPROVE_BOOT` and `CJ2_TRY_CLAIM`.

Two otherwise valid CLAIM calls must each return **`CRAWL_V2_INVALID_STATE`**,
with complete unchanged typed state/absolute expiries and zero accounting delta.
All **46** direct authority probes must return `NOPERM` without state change.
`WRONG_TYPE`, outer `NOPERM`, `NOSCRIPT`, generic/malformed errors or unexpected
success cannot replace the required CLAIM rejection. The second call follows a
definite first outcome; ambiguity stops the case without retry.

One fresh networkless attempt retains **300-second case / 30-second stages /
60-second cleanup**, 128/256/528 MiB init/executor/Redis limits and 400 MiB Redis
maxmemory. Private Unix transport and fresh exclusive control/data volumes are
required. Worker quiescence precedes fresh-helper revocation of all six roles,
with revoker last, followed by exact resource destruction and independent
absence checks. There are zero intended request starts or output commits.

## Approval handoff

Preparation records `execution_authorized=false`. The proposed approval binds
the identities above, operator `fullerkris`, Linux/arm64, one attempt without an
automatic retry, and a one-hour window starting when approval is recorded.
A separate execution decision and final live preflight must precede reservation
and invocation. All prior case approvals remain consumed.

Private workspace:
`/private/var/folders/bg/k5cdrp4s64j32mt9h8b9t39h0000gn/T/opencode/m4-p01-execution-2026-09-24/`.
The intended fresh evidence directory is its `evidence/` child. Approval,
decision, one-use reservation and final disposition originals stay private.

The [primary plan](crawl-jobs-v2-plan.md) owns subsequent status. PC01 remains the
accepted scoped control; all 13 negative cases were still unrun at preparation.
M4-P3/full M4 and final independent release-measurement review remain open.
