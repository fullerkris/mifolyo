services:
  spider-service:
    image: ${MIFOLYO_SPIDER_IMAGE:?MIFOLYO_SPIDER_IMAGE must be the approved spider image at a sha256 digest}
    pull_policy: always
    env_file:
      - path: variables.env
        required: false
    deploy:
      replicas: 1
